version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - misspell
  settings:
    misspell:
      locale: US
      ignore-rules:
        - anual
        - cancelled
